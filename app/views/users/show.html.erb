<div id="myDIV" class="header">
  <h2 class="center"><b><%= current_user.items.count %></b> taSks</h2>

  <%= render partial: 'items/form' %>

  <table id="tableBorder" class="table table-striped table-condensed table-responsive table-hover active" >
    <thead class="thRight">
    <tr>
      <th class="centerText">taSk</th>
      <th class="shrinkColumn centerText">Days remaining</th>
      <th class="shrinkColumn centerText"> Delete</th>
    </tr>
    </thead>
    <tbody>
      <tr>
      <%= render @items %>
    </tr>
    </tbody>
  </table>
</div>
