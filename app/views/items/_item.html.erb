<tr>
  <td class="iTem"><%= item.name %></td>
  <td>
    <% from_time = item.created_at %>
    <% to_time = item.created_at + 7.days %>
    <%= distance_of_time_in_words(from_time, to_time ) %> left <br>
  </td>
  <!-- Edit button WIP -->
  <!-- <td>
  <%= link_to "", item, method: :edit, class: "glyphicon glyphicon-edit" %>
  </td> -->
  <td>
    <% if current_user %>
    <%= link_to "", item, method: :delete, class: 'glyphicon glyphicon-remove blackText' %>
    <% end %>
  </td>
</tr>
